if SHIELD_ROBA_DONGLE

config ZMK_KEYBOARD_NAME
  default "roBa"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
  default y    

config ZMK_POINTING
  default y

endif

if SHIELD_ROBA_DONGLE || SHIELD_ROBA_L || SHIELD_ROBA_R

config ZMK_SPLIT
  default y

endif

if SHIELD_ROBA_R

config SPI
  default y

endif
